# SkyGuard AI — SIH26073
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn==1.8.0
joblib>=1.3
shap>=0.44
streamlit>=1.36
plotly>=5.20
pydeck>=0.9
# Required for the committed Agent-3 model. The backend has an explicit
# deterministic fallback so the dashboard can still start if TensorFlow is absent.
tensorflow>=2.18,<2.21
